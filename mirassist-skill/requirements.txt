# Runtime dependencies for the miRAssist retrieval core (no OpenAI, no torch).
pandas>=2.0
numpy>=1.24
pyarrow>=14.0
requests>=2.31
# Optional: only for live Supabase Postgres mode (EVIDENCE_BACKEND=postgres).
# Default GitHub-snapshot and REST modes need only the above.
sqlalchemy>=2.0
psycopg[binary]>=3.1
